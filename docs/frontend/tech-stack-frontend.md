# フロントエンド技術スタック資料

> このドキュメントは、Smart Household Account Bookプロジェクトのフロントエンドで使用されている技術を学習用にまとめたものです。

## 📋 目次

1. [概要](#概要)
2. [コア技術](#コア技術)
3. [フレームワーク・ライブラリ](#フレームワークライブラリ)
4. [認証・セキュリティ](#認証セキュリティ)
5. [アーキテクチャパターン](#アーキテクチャパターン)
6. [実際のコード例](#実際のコード例)

> **📌 Next.jsフレームワークについて**: Next.js、App Router、Server Componentsなどの詳細は、[Next.jsフレームワーク詳細資料](./nextjs-framework.md)を参照してください。

> **📌 スタイリングとUIライブラリについて**: Tailwind CSS、shadcn/ui、Radix UIなどの詳細は、[スタイリング・UIライブラリ詳細資料](./styling-ui.md)を参照してください。

> **📌 ライブラリの詳細**: React Hook Form、Zod、AWS Amplifyなどの詳細は、[フロントエンドライブラリ詳細資料](./libraries.md)を参照してください。

---

## 概要

このプロジェクトのフロントエンドは、**Next.js 15.5.4**を基盤としたモダンなReactアプリケーションです。TypeScriptで型安全性を確保し、コンポーネント指向の設計で保守性と拡張性を重視しています。

### 主な特徴

- **Next.js 15.5.4**: Reactベースのフルスタックフレームワーク
- **React 19.1.0**: 最新のReact機能を活用
- **TypeScript 5**: 型安全性によるバグの早期発見
- **App Router**: Next.jsの最新ルーティングシステム
- **AWS Amplify**: AWS Cognitoによるセキュアな認証・認可
- **shadcn/ui**: アクセシブルなUIコンポーネントライブラリ

---

## コア技術

### 1. TypeScript 5

**役割**: JavaScriptに型システムを追加したプログラミング言語。フロントエンドの全コードがTypeScriptで記述されています。

**学習ポイント**:
- **型安全性**: コンパイル時にエラーを検出できるため、バグを早期に発見できる
- **IntelliSense**: IDE（Cursor/VS Code）で自動補完やエラー表示が充実
- **リファクタリング**: 型情報があるため、安全にコードを変更できる

**実際のコード例**:

`frontend-nextjs/tsconfig.json` (1-28行目):

```json
{
  "compilerOptions": {
    "target": "ES2017",
    "lib": ["dom", "dom.iterable", "esnext"],
    "allowJs": true,
    "skipLibCheck": true,
    "strict": true,
    "noEmit": true,
    "esModuleInterop": true,
    "module": "esnext",
    "moduleResolution": "bundler",
    "resolveJsonModule": true,
    "isolatedModules": true,
    "jsx": "preserve",
    "incremental": true,
    "plugins": [
      {
        "name": "next"
      }
    ],
    "paths": {
      "@/*": ["./src/*"]
    }
  },
  "include": ["next-env.d.ts", "**/*.ts", "**/*.tsx", ".next/types/**/*.ts"],
  "exclude": ["node_modules"]
}
```

**重要な設定**:
- `"strict": true`: 厳密な型チェックを有効化（推奨）
- `"paths": { "@/*": ["./src/*"] }`: `@/`で`src/`ディレクトリを参照できるエイリアス設定
- `"jsx": "preserve"`: JSXをそのまま保持（Next.jsが処理）

### 2. Next.js 15.5.4

**役割**: Reactアプリケーション開発のためのフルスタックフレームワーク。サーバーサイドレンダリング（SSR）、静的サイト生成（SSG）、APIルートなどの機能を提供します。

> **詳細はこちら**: [Next.js 15.5.4の詳細](./nextjs-framework.md#nextjs-1554)を参照してください。

**主な機能**:
- **App Router**: ファイルベースのルーティングシステム
- **Server Components**: サーバー側でレンダリングされるコンポーネント（デフォルト）
- **Client Components**: ブラウザ側でレンダリングされるコンポーネント（`"use client"`が必要）
- **API Routes**: サーバーサイドのAPIエンドポイントを作成可能

### 3. React 19.1.0

**役割**: ユーザーインターフェースを構築するためのJavaScriptライブラリ。コンポーネントベースのアーキテクチャを採用しています。

**学習ポイント**:
- **コンポーネント**: UIの再利用可能な部品
- **フック（Hooks）**: 関数コンポーネントで状態管理やライフサイクルを扱う
- **仮想DOM**: 効率的なDOM更新を実現

**DOMと仮想DOMの理解**:
- **HTML**: テキストファイル（設計図）。Webページの構造を記述したファイル
- **DOM**: ブラウザがHTMLを読み込んで作成する構造。JavaScriptで操作可能なオブジェクトの階層構造。ブラウザはDOMを基に画面を描画する
- **DOM操作**: JavaScriptでDOMを操作すると、画面が変わる。要素の追加・削除・変更が可能
- **Reactの仮想DOM**: Reactは直接DOMを操作せず、仮想DOM（JavaScriptオブジェクト）で変更を管理し、差分検出により必要な部分だけを効率的に更新する

---

## フレームワーク・ライブラリ

> **📌 ライブラリの詳細**: React Hook Form、Zod、AWS Amplify、OpenAPI Generatorなどの詳細は、[フロントエンドライブラリ詳細資料](./libraries.md)を参照してください。

---

## 認証・セキュリティ

### 1. AWS Amplify

**役割**: AWS Cognitoと連携して、ユーザー認証・認可を管理するライブラリ。

**このプロジェクトでの使用箇所**:
- `src/contexts/auth-provider.tsx`: 認証プロバイダーの設定
- `src/config/aws-exports.ts`: AWS Cognitoの設定
- `src/api/authUtils.ts`: JWTトークンの取得

**学習ポイント**:
- **Authenticatorコンポーネント**: ログイン・サインアップUIを自動提供
- **JWTトークン**: 認証情報を含むトークンを取得してAPIリクエストに付与
- **セッション管理**: 認証状態を自動的に管理

**実際のコード例**:

`frontend-nextjs/src/contexts/auth-provider.tsx` (1-12行目):

```typescript
"use client"

import { Amplify } from "aws-amplify"
import { Authenticator, translations } from "@aws-amplify/ui-react"
import "@aws-amplify/ui-react/styles.css"
import awsConfig from "@/config/aws-exports"
import { I18n } from "aws-amplify/utils"

Amplify.configure(awsConfig)

I18n.putVocabularies(translations)
```

**解説**:
- `"use client"`: このファイルはクライアントコンポーネント（ブラウザで実行）
- `Amplify.configure(awsConfig)`: AWS Cognitoの設定を読み込む
- `Authenticator`: ログイン・サインアップUIを提供するコンポーネント

**参考資料**:
- [AWS Amplify公式ドキュメント](https://docs.amplify.aws/)

---

### 2. JWT認証

**役割**: バックエンドAPIへのリクエスト時に、JWTトークンをAuthorizationヘッダーに付与して認証を行う。

**実際のコード例**:

`frontend-nextjs/src/api/authUtils.ts` (10-30行目):

```typescript
/**
 * CognitoからJWTトークン（IDトークン）を取得
 */
async function getJwtToken(): Promise<string> {
    const session = await fetchAuthSession();
    const token = session.tokens?.idToken?.toString();

    if (!token) {
        throw new Error('認証トークンの取得に失敗しました');
    }
    return token;
}

/**
 * Authorizationヘッダーを付与したオプションオブジェクトを作成
 */
export async function withAuthHeader(): Promise<{ headers: { Authorization: string } }> {
    const token = await getJwtToken();
    return {
        headers: {
            Authorization: `Bearer ${token}`,
        },
    };
}
```

**学習ポイント**:
- **JWTトークン**: 認証情報を含むトークン。バックエンドで検証される
- **Bearer認証**: `Authorization: Bearer <token>`の形式でトークンを送信
- **セッション管理**: `fetchAuthSession()`で現在の認証セッションを取得

---

## アーキテクチャパターン

### コンポーネント指向設計

**役割**: UIを再利用可能なコンポーネントに分割して、保守性と拡張性を向上させる設計手法。

**このプロジェクトでの使用箇所**:
- フロントエンド全体の構造

**ディレクトリ構造**:

1. **`app/`**: Next.js App Routerのページとレイアウト
   - **`page.tsx`**: ページコンポーネント
   - **`layout.tsx`**: レイアウトコンポーネント（全ページ共通）

2. **`src/components/`**: 再利用可能なUIコンポーネント
   - **`ui/`**: shadcn/uiの基本コンポーネント（Button、Inputなど）
   - **`dashboard/`**: ダッシュボード固有のコンポーネント
   - **`expense-form.tsx`**: 支出フォームコンポーネント

3. **`src/hooks/`**: カスタムフック（状態管理やロジックの再利用）
   - **`use-expenses.ts`**: 支出データの取得・更新・削除

4. **`src/api/`**: APIクライアントとユーティリティ
   - **`apiClient.ts`**: APIクライアントの作成
   - **`authUtils.ts`**: 認証ユーティリティ
   - **`generated/`**: OpenAPI Generatorで生成されたコード

5. **`src/lib/`**: ユーティリティ関数と型定義
   - **`utils.ts`**: 汎用ユーティリティ（`cn`関数など）
   - **`types.ts`**: TypeScriptの型定義

6. **`src/contexts/`**: React Context（グローバル状態管理）
   - **`auth-provider.tsx`**: 認証状態のプロバイダー

**実際のコード例（カスタムフック）**:

`frontend-nextjs/src/hooks/use-expenses.ts` (15-32行目):

```typescript
export function useExpenses() {
  const [expenseItems, setExpenses] = useState<Expense[]>([])
  const [isLoaded, setIsLoaded] = useState(false)

  const fetchExpensesList = useCallback(async () => {
    try {
      const expenseList = await fetchExpenses()
      setExpenses(expenseList)
      setIsLoaded(true)
    } catch (error) {
      showApiErrorMessage(error, "支出データの取得に失敗しました")
      setIsLoaded(true)
    }
  }, [])

  useEffect(() => {
    fetchExpensesList()
  }, [fetchExpensesList])
  // ... 他の関数
}
```

**カスタムフックの特徴**:
- **再利用性**: 複数のコンポーネントで同じロジックを共有できる
- **関心の分離**: UIロジックとデータ取得ロジックを分離
- **テスト容易性**: フック単体でテストできる

**学習ポイント**:
- **コンポーネントの分割**: 大きなコンポーネントを小さなコンポーネントに分割
- **カスタムフック**: 状態管理やロジックを再利用可能なフックに抽出
- **型安全性**: TypeScriptでコンポーネントのpropsやstateに型を付ける

---

## 実際のコード例

### ページコンポーネントの実装

`frontend-nextjs/app/layout.tsx` (1-33行目):

```typescript
import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { AuthProvider } from "@/contexts/auth-provider";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "スマート家計簿アプリ",
  description: "AWS Cognitoで保護された家計簿管理アプリケーション",
};

interface RootLayoutProps {
  children: React.ReactNode
}

export default function RootLayout({ children }: Readonly<RootLayoutProps>) {
  return (
    <html lang="ja">
      <body className={`${geistSans.variable} ${geistMono.variable} antialiased`}>
        <AuthProvider>{children}</AuthProvider>
      </body>
    </html>
  )
}
```

**解説**:
- `export const metadata`: Next.jsのメタデータ（SEO用）
- `Geist`, `Geist_Mono`: Google Fontsからフォントを読み込む
- `AuthProvider`: 認証状態を提供するプロバイダーコンポーネント
- **Server Component**: `"use client"`がないため、サーバー側でレンダリングされる

---

### APIクライアントの実装

`frontend-nextjs/src/api/apiClient.ts` (1-32行目):

```typescript
/**
 * APIクライアント作成ユーティリティ
 */

import { Configuration } from './generated/configuration';
import { ExpensesApi, ChatApi } from './generated/api';

/**
 * 環境変数からベースURLを取得
 */
function getBasePath(): string {
    return process.env.NEXT_PUBLIC_API_BASE_URL || '';
}

/**
 * Expense APIクライアントのインスタンスを作成
 */
export function getExpenseApiClient(): ExpensesApi {
    return new ExpensesApi(new Configuration({
        basePath: getBasePath()
    }));
}

/**
 * Chat APIクライアントのインスタンスを作成
 */
export function getChatApiClient(): ChatApi {
    return new ChatApi(new Configuration({
        basePath: getBasePath()
    }));
}
```

**解説**:
- **OpenAPI Generator**: `generated/`配下のコードは自動生成される
- **環境変数**: `NEXT_PUBLIC_API_BASE_URL`でAPIのベースURLを設定
- **型安全性**: 生成されたコードにより、APIの型が保証される

---

### ユーティリティ関数の実装

`frontend-nextjs/src/lib/utils.ts` (1-6行目):

```typescript
import { clsx, type ClassValue } from "clsx"
import { twMerge } from "tailwind-merge"

export function cn(...inputs: ClassValue[]) {
  return twMerge(clsx(inputs))
}
```

**解説**:
- **`cn`関数**: クラス名をマージするユーティリティ関数
- **`clsx`**: 条件付きクラス名を生成
- **`twMerge`**: Tailwind CSSのクラス名をマージ（競合を解決）

**使用例**:
```typescript
<button className={cn("px-4 py-2", isActive && "bg-blue-500")}>
  ボタン
</button>
```

---

## まとめ

このプロジェクトのフロントエンドは、以下の技術スタックで構成されています：

### コア技術
- **TypeScript 5**: プログラミング言語（型安全性）
- **Next.js 15.5.4**: フルスタックフレームワーク
  - 詳細: [Next.js 15.5.4の詳細](./nextjs-framework.md#nextjs-1554)
- **React 19.1.0**: UIライブラリ

### スタイリング
- **Tailwind CSS 4.1.9**: ユーティリティファーストのCSSフレームワーク
  - 詳細: [Tailwind CSSの詳細](./styling-ui.md#tailwind-css-419)
- **shadcn/ui**: アクセシブルなUIコンポーネントライブラリ
  - 詳細: [shadcn/uiの詳細](./styling-ui.md#shadcnui)
- **Radix UI**: アクセシブルなプリミティブコンポーネント
  - 詳細: [Radix UIの詳細](./styling-ui.md#radix-ui)

### 認証・セキュリティ
- **AWS Amplify**: AWS Cognitoとの連携
  - 詳細: [AWS Amplifyの詳細](./libraries.md#aws-amplify)
- **JWT認証**: トークンベース認証

### フォーム・バリデーション
- **React Hook Form**: フォーム状態管理
  - 詳細: [React Hook Formの詳細](./libraries.md#react-hook-form)
- **Zod**: スキーマバリデーション
  - 詳細: [Zodの詳細](./libraries.md#zod)

### 開発ツール
- **OpenAPI Generator**: APIクライアントの自動生成
  - 詳細: [OpenAPI Generatorの詳細](./libraries.md#openapi-generator)
- **ESLint**: コード品質チェック

### アーキテクチャ
- **コンポーネント指向設計**: UIを再利用可能なコンポーネントに分割
- **カスタムフック**: 状態管理やロジックの再利用

これらの技術を組み合わせることで、保守性と拡張性の高いアプリケーションを実現しています。

> **📌 Next.jsフレームワークについて**: Next.js関連の技術の詳細は、[Next.jsフレームワーク詳細資料](./nextjs-framework.md)を参照してください。

---

## 学習の進め方

1. **基礎から学ぶ**: TypeScript、React、Next.jsの基礎を理解する
2. **実際のコードを読む**: このプロジェクトのコードを読んで理解を深める
3. **手を動かす**: 小さな機能を追加して実践する
4. **ドキュメントを読む**: 公式ドキュメントで詳細を確認する

---

## 参考資料

### 公式ドキュメント
- [Next.js公式ドキュメント](https://nextjs.org/docs)
- [React公式ドキュメント](https://react.dev/)
- [TypeScript公式ドキュメント](https://www.typescriptlang.org/docs/)

### 書籍
- [Next.js実践ガイド](https://www.shoeisha.co.jp/book/detail/9784798172025)
- [React実践の教科書](https://www.shoeisha.co.jp/book/detail/9784798162439)

### オンラインコース
- [Next.js公式チュートリアル](https://nextjs.org/learn)

---

**最終更新日**: 2024年

