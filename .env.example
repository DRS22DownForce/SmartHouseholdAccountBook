# コピーして .env にリネームし、値を設定してください（.env は Git に含めません）

# MySQL コンテナ初期化・運用用（Spring には渡さない）
MYSQL_ROOT_PASSWORD=change-me-strong-password
MYSQL_DATABASE=household_book

# Flyway 専用（マイグレーション時だけ Spring が使う）
MYSQL_FLYWAY_USER=flyway_user
MYSQL_FLYWAY_PASSWORD=change-me-flyway-password

# アプリ専用（常時の DataSource）
MYSQL_APP_USER=app_user
MYSQL_APP_PASSWORD=change-me-app-password

# Spring Boot（ローカル IDE / dev 用）
SPRING_DATASOURCE_URL_PROD=jdbc:mysql://mysql:3306/household_book?serverTimezone=UTC&characterEncoding=UTF-8
SPRING_DATASOURCE_URL_DEV=jdbc:mysql://localhost:3306/household_book?serverTimezone=UTC&characterEncoding=UTF-8

# Cognito (JWT 検証)
COGNITO_JWK_SET_URL=
COGNITO_ISSUER_URL=
COGNITO_CLIENT_ID=

# OpenAI
OPENAI_API_KEY=
OPENAI_API_URL=https://api.openai.com/v1/chat/completions

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000
